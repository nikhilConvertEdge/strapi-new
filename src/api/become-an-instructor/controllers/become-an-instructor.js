'use strict';

/**
 * become-an-instructor controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::become-an-instructor.become-an-instructor');

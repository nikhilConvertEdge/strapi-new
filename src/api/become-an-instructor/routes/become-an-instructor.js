'use strict';

/**
 * become-an-instructor router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::become-an-instructor.become-an-instructor');
